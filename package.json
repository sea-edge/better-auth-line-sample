{
  "name": "better-auth-line-sample",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --watch server.ts",
    "start": "bun server.ts",
    "db:migrate": "bunx --bun auth@latest migrate"
  },
  "dependencies": {
    "better-auth": "^1.6.23"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14"
  }
}
